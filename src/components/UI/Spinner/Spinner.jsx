import React from "react";
import classes from "../../../assets/styles/components/spinner.module.css";

const spinner = () => (
    <div className={classes.Loader}>Loading...</div>
);

export default spinner;